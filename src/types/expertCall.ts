export interface ExpertCall {
    icon: React.ReactNode;
    title: string;
    duration: string;
    prefillText: string;
    price?: string;
    description?: string;
    benefits?: string[];
    highlight?: string;
} 